'use strict';

const controllers = require('./controllers/chat-label');
const routes = require('./routes/chat-label');
const services = require('./services/chat-label');

module.exports = {
  controllers,
  routes,
  services,
};
